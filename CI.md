# CI recommendation
Add a GitHub Action:
- install deps
- run `python -m pip install -r requirements.txt`
- run `python -m pytest`
- (optional) ruff + mypy

The coding agent should include this in Milestone 0.
