# dealfinder-vnext

## Repository Migration

If you need to migrate this repository to a new remote origin, see the [Migration Guide](MIGRATION_GUIDE.md).

Alternatively, you can use the automated migration script:

```bash
./migrate-remote.sh <organization> <repository-name>
```

For example:

```bash
./migrate-remote.sh myorg my-dealfinder
```